name: 发布npm package

on:
  push:
    tags:
      - 'v*'

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '18'
          registry-url: 'https://registry.npmjs.org'

      - name: Publish
        run: npm ci
      - run: npm publish --//registry.npmjs.org/:_authToken=$NPM_AUTHTOKEN